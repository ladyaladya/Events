namespace Events.UI.Models
{
    public enum EventTableColumn
    {
        id,
        category,
        name,
        description,
        place,
        time,
        image,
        additional_info
    }
}
